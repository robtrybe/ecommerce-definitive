import sinon from 'sinon';
import sinonChai from 'sinon-chai';
import chai, { expect } from 'chai';

chai.use(sinonChai);

describe('Testes relacionados a rota de criação de usuários', function() {
    it('Testa se 1 é igual a 1', function() {
        expect(1).to.be.equal(2);
    })
})